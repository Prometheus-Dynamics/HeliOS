---
title: Import and Export
---

Import and export moves pipeline graphs between devices (or into/out of your browser downloads folder).

## What you can do

- Export the selected pipeline as JSON (download).
- Import a JSON file to create a new pipeline.

## Where the actions live

- Import: `New Pipeline` -> `Import JSON`.
- Export: in the graph editor actions for the selected pipeline.
