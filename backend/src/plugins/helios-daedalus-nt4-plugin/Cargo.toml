[package]
name = "helios-daedalus-nt4-plugin"
version.workspace = true
edition = "2024"

[lib]
crate-type = ["cdylib"]

[lints]
workspace = true

[dependencies]
daedalus = { workspace = true, features = ["ffi", "plugins", "gpu"] }
lib-nt4-plugin = { path = "../../libs/lib-nt4-plugin" }
