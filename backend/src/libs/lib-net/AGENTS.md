# lib-net

Utilities for network configuration. Functions allow querying and modifying interface properties via `rtnetlink` as well as getting or setting the system hostname. The crate is asynchronous and integrates with Tokio. Data structures are serializable for use over the API.
