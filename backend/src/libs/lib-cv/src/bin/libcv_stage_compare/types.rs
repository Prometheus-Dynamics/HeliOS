#[derive(Debug, Clone)]
pub(crate) struct CandidateQuadsConfig {
    pub(crate) downscale: u32,
    pub(crate) frame_width: u32,
    pub(crate) frame_height: u32,
    pub(crate) epsilon: f32,
    pub(crate) min_area: f32,
    pub(crate) max_area: f32,
    pub(crate) min_angle_deg: f32,
    pub(crate) max_angle_deg: f32,
    pub(crate) max_side_cv: f32,
    pub(crate) max_side_ratio: f32,
    pub(crate) max_diag_ratio: f32,
    pub(crate) mode_fast: bool,
    pub(crate) refine_corners: bool,
    pub(crate) refine_edge_dist: f32,
    pub(crate) refine_min_points: usize,
    pub(crate) min_perimeter_rate: f32,
    pub(crate) max_perimeter_rate: f32,
    pub(crate) min_corner_distance_rate: f32,
    pub(crate) min_distance_to_border_px: f32,
    pub(crate) min_marker_distance_rate: f32,
    pub(crate) simplify_contours: bool,
}

#[derive(Debug, serde::Serialize)]
pub(crate) struct CompareSummary {
    pub(crate) candidates: usize,
    pub(crate) detections: usize,
    pub(crate) mad_mask: Option<f32>,
    pub(crate) mad_mask_inverted: Option<f32>,
    pub(crate) diff_pixels_mask: Option<u64>,
    pub(crate) diff_pixels_mask_inverted: Option<u64>,
    pub(crate) diff_border_mask: Option<u64>,
    pub(crate) diff_interior_mask: Option<u64>,
    pub(crate) diff_border_mask_inverted: Option<u64>,
    pub(crate) diff_interior_mask_inverted: Option<u64>,
    pub(crate) ref_mad_vs_libcv: Option<f32>,
    pub(crate) ref_diff_pixels_vs_libcv: Option<u64>,
    pub(crate) ref_mad_vs_opencv: Option<f32>,
    pub(crate) ref_diff_pixels_vs_opencv: Option<u64>,
    pub(crate) opencv_gray_mad_vs_opencv: Option<f32>,
    pub(crate) opencv_gray_diff_pixels_vs_opencv: Option<u64>,
    pub(crate) opencv_gray_input_mad_vs_opencv: Option<f32>,
    pub(crate) opencv_gray_input_diff_pixels_vs_opencv: Option<u64>,
    pub(crate) filters: FilterStats,
}

#[derive(Debug, Default, serde::Serialize)]
pub(crate) struct FilterStats {
    pub(crate) input_contours: usize,
    pub(crate) rejected_contour_too_short: usize,
    pub(crate) rejected_min_perimeter_rate: usize,
    pub(crate) rejected_max_perimeter_rate: usize,
    pub(crate) rejected_min_perimeter: usize,
    pub(crate) rejected_candidate_quad: usize,
    pub(crate) rejected_quad_not_four: usize,
    pub(crate) rejected_quad_not_convex: usize,
    pub(crate) rejected_quad_angle: usize,
    pub(crate) rejected_quad_area: usize,
    pub(crate) rejected_quad_max_side_cv: usize,
    pub(crate) rejected_quad_max_side_ratio: usize,
    pub(crate) rejected_quad_max_diag_ratio: usize,
    pub(crate) rejected_min_corner_distance: usize,
    pub(crate) rejected_min_border_distance: usize,
    pub(crate) rejected_marker_distance: usize,
    pub(crate) kept_after_basic_filters: usize,
    pub(crate) kept_after_marker_distance: usize,
}
